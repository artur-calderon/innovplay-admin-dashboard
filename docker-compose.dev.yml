services:
  app:
    image: afirmeplay/afirmeplay-frontend:${VERSION}
    container_name: afirmeplay_front_dev
    environment:
      - VITE_API_BASE_URL=$VITE_API_BASE_URL
      - API_CONTAINER=$API_CONTAINER_DEV
    ports:
      - "8080:80"
    networks:
      - dev
    restart: unless-stopped


networks:
  dev:
    driver: bridge